export const HELP = [
  "/start — приветствие",
  "/help — команды",
  "/add_client — клиент",
  "/add_placement — посев",
  "/add_lead — заявка",
  "/clients — список клиентов",
  "/report — потрачено / лиды / CPL",
  "/set_cpl_limit — порог CPL",
].join("\n");
